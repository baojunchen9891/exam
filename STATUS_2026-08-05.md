# 考级诊断网站 · 进度状态（2026-08-05）

## 一、本轮完成的事（用户确认"OK，继续任务"后）

### 1. 固化 8 级考点表确认状态 ✅
- `data/kp/gesp-cpp-L1~L8.json` 共 8 份，全部 `status` 由 `draft` → `confirmed`，写入 `confirmedBy = user-confirmed@2026-08-05`，id 锁定。
- 数量：L1=20 / L2=20 / L3=21 / L4=20 / L5=22 / L6=20 / L7=21 / L8=23（**167 个全局唯一 id**）。

### 2. 权重表补全到全量标准考点 ✅
- 修改 `build/kp_stats.py`：在真实命中（25 个 L4/L5 考点）基础上，把标准考点池里"当前卷未考到"的考点补齐为 `count=0 / ratio=0`。
- 重新生成 `dist/data/kp_weight.json`：覆盖全部 **167 个标准考点**（L4/L5 实际命中 25 个，其余 142 个补齐为 0 权重）。
- 诊断 MVP 仅加载 L4/L5 真题，无关条目权重 0 不影响计算。

### 3. 规则引擎端到端自检 + 修复真实缺陷 ✅
- 用 node 加载真实权重 + `rule_prob.js`，模拟 32 题诊断（全对 / 全错 / 随机对一半）。
- **发现缺陷**：`kp_weight.json` 的 ratio 按考试前缀各自归一（cpp4 合计=1、cpp5 合计=1），混合多级诊断时全局和=2，导致"对一半"也被算成 P=0.98（虚假稳过）。
- **修复**：`computePassProbability` 改为按"本次被测考点集合"重新归一（`Σ(w×m) / Σw`），保证 expectedScore∈[0,1]。
- 修复后断言全过：全对→0.98(稳过)、全错→0.05(风险高)、随机对一半→0.744(较稳)。

## 二、当前就绪度

| 模块 | 状态 | 说明 |
|------|------|------|
| 8 级考点库（167 个） | ✅ 已确认 | id 锁定，可对外发布 |
| L4/L5 真题标注 | 🟡 71% | 324/405 题已标（含 AI 初标），**117 行"未归类"待教研终审** |
| 权重表 kp_weight | ✅ 就绪 | 覆盖全部标准考点 |
| 规则引擎 | ✅ 自检通过 | 含归一化修复 |
| 诊断页 diagnostic.js | ✅ 已解锁 | 守卫看到权重非空即放行，仅跑 L4/L5 |
| CloudBase 阶段3 集合 | ✅ 已建 | diagnoses / teacher_estimates + 索引 |

## 三、开放项 / 待决策

1. **117 行 L4/L5 未归类题**：需教研在腾讯文档标注表复核补全（在线表已含，子表 BB08J2）。完成后重跑 `kp_validate.py --strict` + `kp_backfill.py`，覆盖率可到 100%。
2. **是否标注 L1/2/3/6/7/8 真题**：当前仅考点表就绪，无真题标注。诊断 MVP 锁定四/五级，这 6 级是考点库储备；若要扩展诊断范围，需先准备真题卷 + 标注。
3. **真维度校准（阶段3）**：规则引擎 P 仍是"规则估计值"，需用教师拍区间 + 真实通过率反校准 passLine / 权重（已有 teacherEstimate 采集入口）。

## 四、关键文件
- `data/kp/gesp-cpp-L1~L8.json` — 已确认考点表
- `dist/data/kp_weight.json` — 全量权重
- `dist/assets/js/rule_prob.js` — 规则引擎（已修归一化）
- `build/kp_stats.py` — 权重生成（已补全量）
- `build/kp_validate.py` / `kp_backfill.py` — 标注校验 / 回填
